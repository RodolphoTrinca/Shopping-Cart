import React from 'react';
import { render, screen } from '@testing-library/react';
import Home from '../app/page';

describe('Home', () => {
  it('renders without crashing (mock)', () => {
    // This is a placeholder. In a real test, you would mock fetchProductsFromApi and test product rendering.
    expect(true).toBe(true);
  });
});
